"""Image-side atmosphere rendering for haze-limb (Titan-class) bodies.

Placeholder module reserving the architectural slot: atmospheric rendering
is deliberately not implemented.  The slot exists for an exponential haze
layer (scale height, tangent optical depth, forward-scattering phase
behavior, detached shell) applied as part of body radiance composition; a
body without an ``atmosphere`` block renders hard-limbed.
"""

__all__: list[str] = []
